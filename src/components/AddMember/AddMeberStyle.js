import {StyleSheet} from 'react-native';

const styles = StyleSheet.create({
  addButtonStyle: {
    alignSelf: 'center',
    marginTop: 20,
  },
  addTextStyle: {paddingHorizontal: 20, paddingVertical: 10, borderRadius: 4},
});

export default styles;
